# Abu Master - 开发助手配置

## 开发流程 (自动引导)

当你提出开发需求时，按以下顺序自动执行：

### 1. 需求接收 → 头脑风暴
**触发**：你提出任何开发/修改/功能需求
**动作**：自动调用 `/brainstorm` 进行需求澄清
**目标**：明确要解决什么问题，避免过早进入实现

### 2. 头脑风暴 → 设计计划
**触发**：头脑风暴完成
**动作**：自动调用 `/plan` 编写实施计划
**目标**：拆解任务、确定文件结构、明确步骤

### 3. 计划 → 执行
**触发**：计划编写完成
**动作**：
- 调用 `/subagent-driven` 或 `/executing-plans` 执行任务
- 每个任务完成时自动进行代码审查 `/review`
- 发现问题立即修复

### 4. 完成后 → 测试与提交
**触发**：功能实现完成
**动作**：
- 运行测试验证
- 调用 `/ship` 或 `/commit` 提交代码

## Skills 协作规则

| 场景 | 使用 Skill | 说明 |
|------|-----------|------|
| 提出新需求 | `/brainstorm` | 澄清需求，不急于实现 |
| 开始编码 | `/plan` | 制定详细计划 |
| 执行计划 | `/subagent-driven` (推荐) | 子任务驱动开发 |
| 代码审查 | `/review` | 每次重要修改后自动调用 |
| 调试问题 | `/investigate` | 系统化调试 |
| 提交发布 | `/ship` | 自动发布流程 |

## Karpathy Skills 集成

- `/llmtg` - LLMTG 相关任务时调用
- 其他 karpathy skills 按需调用

## 代码质量标准

- 遵循 DRY、YAGNI、TDD 原则
- 频繁小提交，每个任务完成即提交
- 代码审查发现的问题立即修复

## 项目上下文

- **语言**：Python
- **主要模块**：`cy_quant/` (量化分析), `cy_ai/` (AI分析)
- **数据库**：MySQL，本地连接
- **当前分支**：`update3.9-dev`
